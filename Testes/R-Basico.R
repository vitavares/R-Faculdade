a = 1 # Atrinui ao objeto 'a' o valor 1; 'a' é um vetor de comprimento 1
a # Mostra 'a'

b = 2
b

c = 3
c

a; b; c # Separa as instruções da mesma linha

a+b+c; a-b-c; a*b*c; a/b/c; # Operações aritméticas básicas

a^b # Exponenciação

ls() # Mostrar todos os objetos disponíveis.
rm("c") # Remover objeto 'c'